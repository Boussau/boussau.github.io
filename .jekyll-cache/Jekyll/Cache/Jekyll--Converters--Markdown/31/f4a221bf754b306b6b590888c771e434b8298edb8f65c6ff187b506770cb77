I"�<p>The posterior distribution of the <code class="highlighter-rouge">p_inv</code> indicator. When <code class="highlighter-rouge">p_inv_indicator</code> is 0, the invariable-sites model is “turned off”; when it is 1, it is “turned on”. Therefore, the fraction of samples for which <code class="highlighter-rouge">p_inv_indicator = 1</code> is the posterior probability of the invariable-sites model.</p>
:ET