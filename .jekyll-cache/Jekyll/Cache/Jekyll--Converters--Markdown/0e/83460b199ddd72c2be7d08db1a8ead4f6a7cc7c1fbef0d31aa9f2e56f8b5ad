I"c<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># a log-Exponential prior over the rate of change of a Brownian trait (or a Brownian relaxed clock)
trueTree = readTrees("data/primates.tree")[1]
log_sigma ~ dnLogExponential(lambda=1)
sigma := exp(log_sigma)
X ~ dnBrownian(trueTree,sigma)
# ...
</code></pre></div></div>
:ET