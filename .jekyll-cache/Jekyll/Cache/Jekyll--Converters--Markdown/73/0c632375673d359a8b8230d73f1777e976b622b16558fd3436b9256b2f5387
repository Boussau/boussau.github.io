I"Þ<p>These tutorials will provide command line text and code in specific formats.</p>

<p>All commands that are intended to be executed in your Unix terminal will be shown in gray boxes with the <code class="highlighter-rouge">$</code> prompt. For example:</p>

<div class="bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cd my_directory
pwd
</code></pre></div></div>

<p>All Rev code will be shown in light blue boxes with the <code class="highlighter-rouge">&gt;</code> prompt:</p>

<div class="rev highlighter-rouge"><div class="highlight"><pre class="highlight"><code>n = 12;
for(i in 1:n) {
	#this is a comment
	i++;
}
</code></pre></div></div>

<p>All output the RevBayes console will be given in light blue boxes with gray text:</p>

<div class="rev-output highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/home/my_directory
</code></pre></div></div>

<p>Some tutorials will provide instructions for you that will be highlighted by a box. These instructions will guide you to create new files or other tasks that are not completed in the RevBayes console.</p>

<blockquote class="instruction">
  <p>Make a new file called <code class="highlighter-rouge">m_gtr.Rev</code>.</p>
</blockquote>

:ET